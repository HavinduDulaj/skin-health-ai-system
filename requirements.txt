pillow>=10.0.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
imagehash>=4.3.1
opencv-python-headless>=4.8.0,<5
scikit-learn>=1.3.0
tqdm>=4.66.0
PyYAML>=6.0
torch>=2.1.0
torchvision>=0.16.0
matplotlib>=3.8.0
seaborn>=0.13.0
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
keras>=3.12.0,<3.13
h5py>=3.10.0
# Keras 3 needs a backend. Torch (above) is used automatically when TensorFlow
# is unavailable, e.g. on Python 3.14, which has no TensorFlow wheel.
# Grad-CAM and inference work on either backend.
# tensorflow>=2.16 ; python_version < "3.14"

